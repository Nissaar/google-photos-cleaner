# Actions are pinned to commit SHAs so a moved tag cannot change what runs. This
# keeps those pins current: Dependabot opens a PR when a new release is out.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
